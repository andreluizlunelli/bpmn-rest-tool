{% extends './layout/layout-standard.twig' %}

{% block content %}{{ parent() }}
<div class="container">
    <form method="post" enctype="multipart/form-data" class="mt-3">
        <header>
            <h3>Passos para enviar o arquivo:</h3>
        </header>
        <div class="list-group">
            <a href="#" class="list-group-item list-group-item-action">
                <div class="d-flex w-100 justify-content-between">
                    <h6 class="mb-1">Abra seu projeto no Microsoft Project</h6>
                </div>
            </a>
            <a href="#" class="list-group-item list-group-item-action">
                <div class="d-flex w-100 justify-content-between">
                    <h6 class="mb-1">Salve seu projeto com a extensão de arquivo .xml</h6>
                </div>
                <p class="mb-1">
                    <img src="/images/como-salvar.png" />
                </p>
            </a>
            <a href="#" class="list-group-item list-group-item-action">
                <div class="d-flex w-100 justify-content-between">
                    <h6 class="mb-1">Agora é só <span class="badge badge-primary">enviar</span> o formulário</h6>
                </div>
            </a>
        </div>
        <div class="form-group mt-3">
            <label>Seu .xml aqui </label>
            <input type="file" class="form-control" name="xml">
        </div>
        <input type="submit" value="Enviar" class="btn btn-primary " />
        <div class="alert alert-warning mt-3" role="alert">
            Como restrição, toda lista de tarefas deve ser envolvido por um sub-processo
        </div>
    </form>
</div>
{% endblock %}
